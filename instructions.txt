pip install -r requirements.txt

# Full pipeline: train → evaluate → latency benchmark
python run.py --labels /path/to/labels.pkl --video_root /path/to/sessions/

# Monitor training live in a separate terminal
python live_monitor.py

# Re-plot curves from a finished run
python run.py --plot_only

# Run only evaluation + latency benchmark on a saved model
python run.py --eval_only --labels /path/to/labels.pkl --video_root /path/to/sessions/